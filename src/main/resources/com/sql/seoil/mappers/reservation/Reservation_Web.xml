<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.seoil.campustown.review.service.impl.ReviewMapper">
	
	<select id="selectReservationServiceInfo" parameterType="int" resultType="storeVO">
		select rn_num
			  ,r.u_id
			  ,r.s_num
			  ,rn_count
			  ,rn_date
			  ,rn_regdate
			  ,rn_name
			  ,u.u_id
			  ,s.s_num
	      from reservation r
	      join user u
	        on r.u_id = u.u_id
	      join store  s
	      	on r.s_num = s.s_num
		 where s_num = #{_parameter}
	</select>
	
	<insert id="insertReservationServiceInfo" parameterType="reviewVO">
		<![CDATA[
			insert into reservation(
				u_id    , s_num, rn_count,
				rn_date, rn_regdate, rn_content, rn_name
			) values (
				#{u_id} , #{s_num}, #{rn_count}, 
				#{rn_date}, now(), #{rn_content}, #{rn_name} 
			)
		]]>
	</insert>
	
	<delete id="deleteReservationServiceInfo" parameterType="int">
		delete from reservation
		 where rn_num = #{_parameter}  
	</delete>
	
</mapper>
	