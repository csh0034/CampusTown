<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.seoil.campustown.review.service.impl.ReviewMapper">
	
	<select id="selectReviewServiceList" resultType="reviewVO">
		select rw_num, rw.u_id, s.s_num, rw_content, rw_score, rw_regdate, s_name
	 	  from review rw join store s on rw.s_num = s.s_num
	 	 <if test="_parameter > 0">
	 	 	where s.s_num = #{_parameter}
	 	 </if>
	     order by rw_regdate desc
	</select>
	
	<insert id="insertReviewServiceInfo" parameterType="reviewVO">
		<![CDATA[
			insert into review(
				u_id    , s_num, rw_content,
				rw_score, rw_regdate
			) values (
				#{u_id} , #{s_num}, #{rw_content},
				#{rw_score}, now()
			)
		]]>
	</insert>
	
	<delete id="deleteReviewServiceInfo" parameterType="int">
		delete from review
		 where rw_num = #{_parameter}  
	</delete>
	
</mapper>