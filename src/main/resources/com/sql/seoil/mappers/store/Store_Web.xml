<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.seoil.campustown.store.service.impl.StoreMapper">

	<select id="selectStoreServiceInfo" resultType="map">
		select sysdate()
	</select>
	
	<insert id="saveStoreServiceInfo" parameterType="storeVO">
		insert into store(
			s_name, c_num   , s_tel    , s_address, s_content,
			s_img , s_snsurl, s_hashtag, s_hours
		) values (
			#{s_name}, #{c_num}   , #{s_tel}    , #{s_address}, #{s_content},
			#{s_img} , #{s_snsurl}, #{s_hashtag}, #{s_hours}
		)
	</insert>
	

</mapper>